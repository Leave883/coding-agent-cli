# coding-agent-cli
更新学习中，目前实现：

实现 / 命令：实现五个命令，通过 /help 查看所有命令

实现逐步输出：把 run_sync() 换成 agent.iter() 来实现 Agent 循环的逐步输出

遇见错误不崩溃：分析环境错误（透明重试） or 模型行为错误（错误送进上下文）。增加 API、工具和主循环三层错误处理。

增加会话管理：每轮对话结束后将消息追加到 jsonl。实现 /resume，恢复历史对话。

